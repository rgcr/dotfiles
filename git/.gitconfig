[init]
    defaultBranch = main
[pull]
    ff = true
    rebase = true
[credential]
    helper = cache --timeout=999999
# Aliases and machine-local overrides
[include]
    path = ~/.gitalias
    path = ~/.gitconfig.local
